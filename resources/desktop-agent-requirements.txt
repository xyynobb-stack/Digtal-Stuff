# Installed only while GitHub Actions builds the bundled Agent environment.
# Runtime startup never downloads or installs these dependencies.
primp==1.3.1
ddgs==9.16.0
# Feishu/Lark SDK for the messaging adapter. Personal Drive uses the OAuth proxy.
lark-oapi==1.6.8
